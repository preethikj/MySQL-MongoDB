CREATE TABLE employee_jdbc(empcode INT PRIMARY KEY,
							empname VARCHAR(100),
                            empage INT,
                            salary DECIMAL(7,2));
                            
SELECT * FROM employee_jdbc;

